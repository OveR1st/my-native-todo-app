import { Text, View } from 'react-native'
import type { ScreenProps } from 'shared'
export const CompleteScreen: React.FC<ScreenProps<'Complete'>> = () => {
    return (
        <View>
            <Text>COMPLETE</Text>
        </View>
    )
}
